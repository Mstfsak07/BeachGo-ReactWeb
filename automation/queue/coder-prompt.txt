

Task:

# FAZ 3 â Kritik HatalarÄ±n GerÃ§ek Dosya YazÄ±mÄ±yla DÃ¼zeltilmesi

## Durum Analizi

Ãnceki 2 iterasyonda planlama yapÄ±ldÄ± ancak executor fiziksel dosya yazÄ±mÄ± gerÃ§ekleÅtiremedi. Executor "Plan Mode" kÄ±sÄ±tlamasÄ± nedeniyle dosyalarÄ± deÄiÅtiremedi. Bu fazda executor'a doÄrudan, adÄ±m adÄ±m dosya yazma talimatÄ± verilecek.

## AmaÃ§

3 kritik hatayÄ± fiziksel olarak dÃ¼zeltmek:
1. `axios.js` â `refreshAccessToken` export edilmeli
2. `DashboardReservations.jsx` â `initialState` tanÄ±mlanmalÄ±
3. `AuthContext.jsx` â import dÃ¼zeltmesi (gerekirse)

---

## Executor TalimatlarÄ±

### AdÄ±m 1: `axios.js` DosyasÄ±nÄ± Oku ve DÃ¼zelt

DosyayÄ± oku:
```
/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js
```

DosyanÄ±n en altÄ±na (son satÄ±rÄ±n arkasÄ±na) Åu kodu ekle:

```javascript
export const refreshAccessToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token');
  const response = await axiosInstance.post('/auth/refresh', { refreshToken });
  const { accessToken } = response.data;
  localStorage.setItem('accessToken', accessToken);
  return accessToken;
};
```

**Not:** Dosyada mevcut axios instance'Ä±nÄ±n adÄ± `axiosInstance` deÄilse (Ã¶rneÄin `api` veya `instance` ise), o ismi kullan. DosyayÄ± okuyunca anlarsÄ±n.

---

### AdÄ±m 2: `DashboardReservations.jsx` DosyasÄ±nÄ± Oku ve DÃ¼zelt

DosyayÄ± oku:
```
/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx
```

Import bloÄunun hemen altÄ±na, component fonksiyonu baÅlamadan Ã¶nce Åu satÄ±rlarÄ± ekle:

```javascript
const initialState = {
  search: '',
  filterType: 'All',
  filterStatus: 'All',
  sortType: 'Newest',
  currentPage: 1
};
```

---

### AdÄ±m 3: `AuthContext.jsx` DosyasÄ±nÄ± Kontrol Et

DosyayÄ± oku:
```
/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/context/AuthContext.jsx
```

Åunu kontrol et: DosyanÄ±n Ã¼st kÄ±smÄ±nda `refreshAccessToken`'Ä±n import edildiÄi satÄ±r var mÄ±?

- **Varsa**: DeÄiÅiklik yapma.
- **Yoksa**: Mevcut API import satÄ±rÄ±na `refreshAccessToken`'Ä± ekle. ÃrneÄin:
  ```javascript
  import api, { setAccessToken, clearAccessToken, refreshAccessToken } from '../api/axios';
  ```

---

### AdÄ±m 4: Yazma Ä°Ålemini DoÄrula

Her dosyayÄ± dÃ¼zenledikten sonra dosyayÄ± tekrar oku ve deÄiÅikliÄin kaydedildiÄini doÄrula:
- `axios.js` â `refreshAccessToken` export satÄ±rÄ± gÃ¶rÃ¼nmeli
- `DashboardReservations.jsx` â `const initialState = {` satÄ±rÄ± gÃ¶rÃ¼nmeli
- `AuthContext.jsx` â import satÄ±rÄ± doÄru gÃ¶rÃ¼nmeli

---

## Etkilenen Dosyalar

| Dosya | DeÄiÅiklik |
|-------|-----------|
| `src/api/axios.js` | `refreshAccessToken` export ekleme |
| `src/pages/DashboardReservations.jsx` | `initialState` const tanÄ±mlama |
| `src/context/AuthContext.jsx` | Import kontrolÃ¼ ve gerekirse dÃ¼zeltme |

## Test SenaryolarÄ±

- `axios.js` dosyasÄ±nda `export const refreshAccessToken` satÄ±rÄ± mevcut olmalÄ±
- `DashboardReservations.jsx` baÅÄ±nda `const initialState = {` mevcut olmalÄ±
- `AuthContext.jsx` import satÄ±rÄ±nda `refreshAccessToken` adÄ± bulunmalÄ±

## Riskler

- Executor'Ä±n dosya yazma aracÄ±na eriÅimi yoksa bu faz yine baÅarÄ±sÄ±z olacak â Bir sonraki iterasyonda farklÄ± yaklaÅÄ±m denenecek (shell script ile sed komutu)

<commit_message>fix: axios.js refreshAccessToken export, DashboardReservations initialState ve AuthContext import dÃ¼zeltildi</commit_message>


# FAZ 3 â Kritik HatalarÄ±n GerÃ§ek Dosya YazÄ±mÄ±yla DÃ¼zeltilmesi

## Durum Analizi

Ãnceki 2 iterasyonda planlama yapÄ±ldÄ± ancak executor fiziksel dosya yazÄ±mÄ± gerÃ§ekleÅtiremedi. Executor "Plan Mode" kÄ±sÄ±tlamasÄ± nedeniyle dosyalarÄ± deÄiÅtiremedi. Bu fazda executor'a doÄrudan, adÄ±m adÄ±m dosya yazma talimatÄ± verilecek.

## AmaÃ§

3 kritik hatayÄ± fiziksel olarak dÃ¼zeltmek:
1. `axios.js` â `refreshAccessToken` export edilmeli
2. `DashboardReservations.jsx` â `initialState` tanÄ±mlanmalÄ±
3. `AuthContext.jsx` â import dÃ¼zeltmesi (gerekirse)

---

## Executor TalimatlarÄ±

### AdÄ±m 1: `axios.js` DosyasÄ±nÄ± Oku ve DÃ¼zelt

DosyayÄ± oku:
```
/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js
```

DosyanÄ±n en altÄ±na (son satÄ±rÄ±n arkasÄ±na) Åu kodu ekle:

```javascript
export const refreshAccessToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token');
  const response = await axiosInstance.post('/auth/refresh', { refreshToken });
  const { accessToken } = response.data;
  localStorage.setItem('accessToken', accessToken);
  return accessToken;
};
```

**Not:** Dosyada mevcut axios instance'Ä±nÄ±n adÄ± `axiosInstance` deÄilse (Ã¶rneÄin `api` veya `instance` ise), o ismi kullan. DosyayÄ± okuyunca anlarsÄ±n.

---

### AdÄ±m 2: `DashboardReservations.jsx` DosyasÄ±nÄ± Oku ve DÃ¼zelt

DosyayÄ± oku:
```
/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx
```

Import bloÄunun hemen altÄ±na, component fonksiyonu baÅlamadan Ã¶nce Åu satÄ±rlarÄ± ekle:

```javascript
const initialState = {
  search: '',
  filterType: 'All',
  filterStatus: 'All',
  sortType: 'Newest',
  currentPage: 1
};
```

---

### AdÄ±m 3: `AuthContext.jsx` DosyasÄ±nÄ± Kontrol Et

DosyayÄ± oku:
```
/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/context/AuthContext.jsx
```

Åunu kontrol et: DosyanÄ±n Ã¼st kÄ±smÄ±nda `refreshAccessToken`'Ä±n import edildiÄi satÄ±r var mÄ±?

- **Varsa**: DeÄiÅiklik yapma.
- **Yoksa**: Mevcut API import satÄ±rÄ±na `refreshAccessToken`'Ä± ekle. ÃrneÄin:
  ```javascript
  import api, { setAccessToken, clearAccessToken, refreshAccessToken } from '../api/axios';
  ```

---

### AdÄ±m 4: Yazma Ä°Ålemini DoÄrula

Her dosyayÄ± dÃ¼zenledikten sonra dosyayÄ± tekrar oku ve deÄiÅikliÄin kaydedildiÄini doÄrula:
- `axios.js` â `refreshAccessToken` export satÄ±rÄ± gÃ¶rÃ¼nmeli
- `DashboardReservations.jsx` â `const initialState = {` satÄ±rÄ± gÃ¶rÃ¼nmeli
- `AuthContext.jsx` â import satÄ±rÄ± doÄru gÃ¶rÃ¼nmeli

---

## Etkilenen Dosyalar

| Dosya | DeÄiÅiklik |
|-------|-----------|
| `src/api/axios.js` | `refreshAccessToken` export ekleme |
| `src/pages/DashboardReservations.jsx` | `initialState` const tanÄ±mlama |
| `src/context/AuthContext.jsx` | Import kontrolÃ¼ ve gerekirse dÃ¼zeltme |

## Test SenaryolarÄ±

- `axios.js` dosyasÄ±nda `export const refreshAccessToken` satÄ±rÄ± mevcut olmalÄ±
- `DashboardReservations.jsx` baÅÄ±nda `const initialState = {` mevcut olmalÄ±
- `AuthContext.jsx` import satÄ±rÄ±nda `refreshAccessToken` adÄ± bulunmalÄ±

## Riskler

- Executor'Ä±n dosya yazma aracÄ±na eriÅimi yoksa bu faz yine baÅarÄ±sÄ±z olacak â Bir sonraki iterasyonda farklÄ± yaklaÅÄ±m denenecek (shell script ile sed komutu)

<commit_message>fix: axios.js refreshAccessToken export, DashboardReservations initialState ve AuthContext import dÃ¼zeltildi</commit_message>


Final response requirements:
- Briefly list the files changed.
- Mention any remaining issue or follow-up work.
- Show the git commit message used.
