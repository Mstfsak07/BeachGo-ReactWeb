You have access to a browser via the BrowserMCP tool. 
After making code changes:
1. Use browser_navigate to open http://localhost:3000
2. Check the page visually for errors
3. Test the specific feature you just implemented
4. Report what you saw in the browser

Task:

# FAZ 1: Authentication AkÄ±ÅÄ± - Email DoÄrulama ve Åifre SÄ±fÄ±rlama Backend KontrolÃ¼

## SCOPE: api

---

## GÃREV

Mevcut API projesindeki authentication endpoint'lerini doÄrula ve eksikleri tamamla.

---

## ADIM 1: Mevcut DosyalarÄ± Oku ve Analiz Et

Ãnce Åu dosyalarÄ± oku (varsa):
- `/AuthController.cs` veya `Controllers/AuthController.cs`
- `/Services/AuthService.cs` veya `AuthService.cs`
- `/Services/OtpService.cs` veya `OtpService.cs`
- `/Models/AuthModels.cs` veya `AuthModels.cs`
- `Program.cs` (FallbackPolicy konfigÃ¼rasyonunu gÃ¶rmek iÃ§in)

Projenin kÃ¶k dizinini bul: `find . -name "*.csproj" -not -path "*/node_modules/*"` komutu ile.

---

## ADIM 2: AuthModels.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Models/AuthModels.cs` (yoksa oluÅtur)

Åu request/response model sÄ±nÄ±flarÄ±nÄ±n var olduÄundan emin ol:

```
// OLMASI GEREKENLER:
- RegisterRequest       { Email, Password, FirstName, LastName, PhoneNumber }
- LoginRequest          { Email, Password }
- LoginResponse         { Token, RefreshToken, ExpiresAt, User }
- ForgotPasswordRequest { Email }
- ResetPasswordRequest  { Email, OtpCode, NewPassword }
- VerifyEmailRequest    { Email, OtpCode }
- AuthResponse          { Success, Message }
- UserDto               { Id, Email, FirstName, LastName, PhoneNumber, IsEmailVerified }
```

Eksik olan modelleri ekle.

---

## ADIM 3: IAuthService.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Services/IAuthService.cs` (yoksa oluÅtur)

Interface'de Åu method signature'larÄ±n olduÄundan emin ol:

```
Task<AuthResponse> RegisterAsync(RegisterRequest request);
Task<LoginResponse> LoginAsync(LoginRequest request);
Task<AuthResponse> ForgotPasswordAsync(ForgotPasswordRequest request);
Task<AuthResponse> ResetPasswordAsync(ResetPasswordRequest request);
Task<AuthResponse> VerifyEmailAsync(VerifyEmailRequest request);
```

---

## ADIM 4: IOtpService.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Services/IOtpService.cs` (yoksa oluÅtur)

```
Task<string> GenerateOtpAsync(string email, OtpPurpose purpose);
Task<bool> ValidateOtpAsync(string email, string otpCode, OtpPurpose purpose);
```

`OtpPurpose` enum: `EmailVerification, PasswordReset`

---

## ADIM 5: VerificationCode.cs Entity KontrolÃ¼

**Dosya:** `[proje_koku]/Models/VerificationCode.cs` veya `Entities/VerificationCode.cs`

```
public class VerificationCode {
    public int Id { get; set; }
    public string Email { get; set; }
    public string Code { get; set; }
    public OtpPurpose Purpose { get; set; }
    public DateTime ExpiresAt { get; set; }
    public bool IsUsed { get; set; }
    public DateTime CreatedAt { get; set; }
}
```

---

## ADIM 6: OtpService.cs Implementasyonu KontrolÃ¼

**Dosya:** `[proje_koku]/Services/OtpService.cs`

`GenerateOtpAsync` method'u:
- 6 haneli random kod Ã¼retmeli
- VerificationCode entity'sini DB'ye kaydetmeli (EF Core)
- AynÄ± email + purpose iÃ§in eski kodlarÄ± `IsUsed = true` yapmalÄ±
- Kodu MockSmsService veya loglayarak "gÃ¶ndermeli" (gerÃ§ek SMS deÄil)

`ValidateOtpAsync` method'u:
- DB'den email + purpose + IsUsed=false koÅuluyla kodu bul
- ExpiresAt > DateTime.UtcNow kontrolÃ¼ yap
- GeÃ§erliyse IsUsed = true yap ve kaydet
- true/false dÃ¶ndÃ¼r

---

## ADIM 7: AuthController.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Controllers/AuthController.cs`

Åu endpoint'lerin TAMAMININ var olduÄundan emin ol:

```csharp
[AllowAnonymous]
[HttpPost("register")]
public async Task<IActionResult> Register([FromBody] RegisterRequest request)

[AllowAnonymous]
[HttpPost("login")]
public async Task<IActionResult> Login([FromBody] LoginRequest request)

[AllowAnonymous]
[HttpPost("forgot-password")]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)

[AllowAnonymous]
[HttpPost("reset-password")]
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)

[AllowAnonymous]
[HttpPost("verify-email")]
public async Task<IActionResult> VerifyEmail([FromBody] VerifyEmailRequest request)
```

**KRÄ°TÄ°K:** Controller sÄ±nÄ±fÄ±nÄ±n baÅÄ±nda `[ApiController]` ve `[Route("api/auth")]` attribute'larÄ± olmalÄ±.

Her endpoint'te `[AllowAnonymous]` attribute'Ä± ZORUNLU (FallbackPolicy = RequireAuthenticatedUser olduÄu iÃ§in).

---

## ADIM 8: Program.cs KontrolÃ¼

`Program.cs` iÃ§inde ÅunlarÄ± doÄrula:

```csharp
// Bu satÄ±r olmalÄ± - FallbackPolicy kontrolÃ¼:
options.FallbackPolicy = new AuthorizationPolicyBuilder()
    .RequireAuthenticatedUser()
    .Build();
```

EÄer bu varsa, tÃ¼m auth endpoint'lerinde `[AllowAnonymous]` Åart. Yoksa ekle (ama mevcut yapÄ±yÄ± bozmadan).

---

## ADIM 9: Build ve Test

```bash
cd [proje_koku]
dotnet build
```

Build hatasÄ±z tamamlanmalÄ±.

---

## BEKLENEN SONUÃ

- `dotnet build` â 0 error
- Swagger UI'da gÃ¶rÃ¼nmeli:
  - `POST /api/auth/register`
  - `POST /api/auth/login`
  - `POST /api/auth/forgot-password`
  - `POST /api/auth/reset-password`
  - `POST /api/auth/verify-email`
- Her endpoint `200 OK` veya `400 Bad Request` dÃ¶nebilmeli

---

<commit_message>feat: auth endpoints verified - verify-email, forgot-password, reset-password with AllowAnonymous attributes and OTP service integration</commit_message>


# FAZ 1: Authentication AkÄ±ÅÄ± - Email DoÄrulama ve Åifre SÄ±fÄ±rlama Backend KontrolÃ¼

## SCOPE: api

---

## GÃREV

Mevcut API projesindeki authentication endpoint'lerini doÄrula ve eksikleri tamamla.

---

## ADIM 1: Mevcut DosyalarÄ± Oku ve Analiz Et

Ãnce Åu dosyalarÄ± oku (varsa):
- `/AuthController.cs` veya `Controllers/AuthController.cs`
- `/Services/AuthService.cs` veya `AuthService.cs`
- `/Services/OtpService.cs` veya `OtpService.cs`
- `/Models/AuthModels.cs` veya `AuthModels.cs`
- `Program.cs` (FallbackPolicy konfigÃ¼rasyonunu gÃ¶rmek iÃ§in)

Projenin kÃ¶k dizinini bul: `find . -name "*.csproj" -not -path "*/node_modules/*"` komutu ile.

---

## ADIM 2: AuthModels.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Models/AuthModels.cs` (yoksa oluÅtur)

Åu request/response model sÄ±nÄ±flarÄ±nÄ±n var olduÄundan emin ol:

```
// OLMASI GEREKENLER:
- RegisterRequest       { Email, Password, FirstName, LastName, PhoneNumber }
- LoginRequest          { Email, Password }
- LoginResponse         { Token, RefreshToken, ExpiresAt, User }
- ForgotPasswordRequest { Email }
- ResetPasswordRequest  { Email, OtpCode, NewPassword }
- VerifyEmailRequest    { Email, OtpCode }
- AuthResponse          { Success, Message }
- UserDto               { Id, Email, FirstName, LastName, PhoneNumber, IsEmailVerified }
```

Eksik olan modelleri ekle.

---

## ADIM 3: IAuthService.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Services/IAuthService.cs` (yoksa oluÅtur)

Interface'de Åu method signature'larÄ±n olduÄundan emin ol:

```
Task<AuthResponse> RegisterAsync(RegisterRequest request);
Task<LoginResponse> LoginAsync(LoginRequest request);
Task<AuthResponse> ForgotPasswordAsync(ForgotPasswordRequest request);
Task<AuthResponse> ResetPasswordAsync(ResetPasswordRequest request);
Task<AuthResponse> VerifyEmailAsync(VerifyEmailRequest request);
```

---

## ADIM 4: IOtpService.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Services/IOtpService.cs` (yoksa oluÅtur)

```
Task<string> GenerateOtpAsync(string email, OtpPurpose purpose);
Task<bool> ValidateOtpAsync(string email, string otpCode, OtpPurpose purpose);
```

`OtpPurpose` enum: `EmailVerification, PasswordReset`

---

## ADIM 5: VerificationCode.cs Entity KontrolÃ¼

**Dosya:** `[proje_koku]/Models/VerificationCode.cs` veya `Entities/VerificationCode.cs`

```
public class VerificationCode {
    public int Id { get; set; }
    public string Email { get; set; }
    public string Code { get; set; }
    public OtpPurpose Purpose { get; set; }
    public DateTime ExpiresAt { get; set; }
    public bool IsUsed { get; set; }
    public DateTime CreatedAt { get; set; }
}
```

---

## ADIM 6: OtpService.cs Implementasyonu KontrolÃ¼

**Dosya:** `[proje_koku]/Services/OtpService.cs`

`GenerateOtpAsync` method'u:
- 6 haneli random kod Ã¼retmeli
- VerificationCode entity'sini DB'ye kaydetmeli (EF Core)
- AynÄ± email + purpose iÃ§in eski kodlarÄ± `IsUsed = true` yapmalÄ±
- Kodu MockSmsService veya loglayarak "gÃ¶ndermeli" (gerÃ§ek SMS deÄil)

`ValidateOtpAsync` method'u:
- DB'den email + purpose + IsUsed=false koÅuluyla kodu bul
- ExpiresAt > DateTime.UtcNow kontrolÃ¼ yap
- GeÃ§erliyse IsUsed = true yap ve kaydet
- true/false dÃ¶ndÃ¼r

---

## ADIM 7: AuthController.cs KontrolÃ¼

**Dosya:** `[proje_koku]/Controllers/AuthController.cs`

Åu endpoint'lerin TAMAMININ var olduÄundan emin ol:

```csharp
[AllowAnonymous]
[HttpPost("register")]
public async Task<IActionResult> Register([FromBody] RegisterRequest request)

[AllowAnonymous]
[HttpPost("login")]
public async Task<IActionResult> Login([FromBody] LoginRequest request)

[AllowAnonymous]
[HttpPost("forgot-password")]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)

[AllowAnonymous]
[HttpPost("reset-password")]
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)

[AllowAnonymous]
[HttpPost("verify-email")]
public async Task<IActionResult> VerifyEmail([FromBody] VerifyEmailRequest request)
```

**KRÄ°TÄ°K:** Controller sÄ±nÄ±fÄ±nÄ±n baÅÄ±nda `[ApiController]` ve `[Route("api/auth")]` attribute'larÄ± olmalÄ±.

Her endpoint'te `[AllowAnonymous]` attribute'Ä± ZORUNLU (FallbackPolicy = RequireAuthenticatedUser olduÄu iÃ§in).

---

## ADIM 8: Program.cs KontrolÃ¼

`Program.cs` iÃ§inde ÅunlarÄ± doÄrula:

```csharp
// Bu satÄ±r olmalÄ± - FallbackPolicy kontrolÃ¼:
options.FallbackPolicy = new AuthorizationPolicyBuilder()
    .RequireAuthenticatedUser()
    .Build();
```

EÄer bu varsa, tÃ¼m auth endpoint'lerinde `[AllowAnonymous]` Åart. Yoksa ekle (ama mevcut yapÄ±yÄ± bozmadan).

---

## ADIM 9: Build ve Test

```bash
cd [proje_koku]
dotnet build
```

Build hatasÄ±z tamamlanmalÄ±.

---

## BEKLENEN SONUÃ

- `dotnet build` â 0 error
- Swagger UI'da gÃ¶rÃ¼nmeli:
  - `POST /api/auth/register`
  - `POST /api/auth/login`
  - `POST /api/auth/forgot-password`
  - `POST /api/auth/reset-password`
  - `POST /api/auth/verify-email`
- Her endpoint `200 OK` veya `400 Bad Request` dÃ¶nebilmeli

---

<commit_message>feat: auth endpoints verified - verify-email, forgot-password, reset-password with AllowAnonymous attributes and OTP service integration</commit_message>


Final response requirements:
- Briefly list the files changed.
- Mention any remaining issue or follow-up work.
- Show the git commit message used.
