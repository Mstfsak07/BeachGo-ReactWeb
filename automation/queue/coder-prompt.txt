

Task:

# FAZ 2 â Kritik HatalarÄ±n DÃ¼zeltilmesi

## AmaÃ§
Analiz fazÄ±nda tespit edilen 3 kritik hatayÄ± dÃ¼zeltmek:
1. `axios.js` dosyasÄ±nda `refreshAccessToken` fonksiyonunun eksik olmasÄ±
2. `DashboardReservations.jsx` dosyasÄ±nda `initialState` tanÄ±msÄ±zlÄ±ÄÄ±
3. `AuthContext.jsx` iÃ§indeki import hatasÄ±

## Executor'a Talimatlar

AÅaÄÄ±daki dosyalarÄ± sÄ±rasÄ±yla oku, analiz et ve dÃ¼zelt.

---

### AdÄ±m 1: DosyalarÄ± Oku

Ãnce Åu dosyalarÄ±n tam iÃ§eriÄini oku:

1. `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js`
2. `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/context/AuthContext.jsx`
3. `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx`

---

### AdÄ±m 2: `axios.js` DÃ¼zeltmesi

DosyayÄ± okuduktan sonra ÅunlarÄ± kontrol et ve dÃ¼zelt:

- EÄer `refreshAccessToken` adÄ±nda bir export edilen fonksiyon **yoksa**, dosyanÄ±n en altÄ±na Åunu ekle (mevcut axios instance yapÄ±sÄ±nÄ± bozmadan):

```javascript
export const refreshAccessToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token');
  
  const response = await axios.post('/auth/refresh', { refreshToken });
  const { accessToken } = response.data;
  localStorage.setItem('accessToken', accessToken);
  return accessToken;
};
```

- EÄer fonksiyon zaten varsa ama export edilmiyorsa, `export` keyword'Ã¼nÃ¼ ekle.
- Mevcut interceptor'lar varsa bozma, sadece eksik olan kÄ±smÄ± ekle.

---

### AdÄ±m 3: `AuthContext.jsx` DÃ¼zeltmesi

DosyayÄ± okuduktan sonra:

- `refreshAccessToken`'Ä±n nereden import edildiÄine bak.
- EÄer `import { refreshAccessToken } from '../api/axios'` Åeklinde import varsa ama axios.js'de bu export yoktu, artÄ±k eklendiÄi iÃ§in sorun Ã§Ã¶zÃ¼lmÃ¼Å olacak.
- EÄer import satÄ±rÄ± hiÃ§ yoksa, dosyanÄ±n Ã¼st kÄ±smÄ±ndaki import bloÄuna Åunu ekle:
  ```javascript
  import { refreshAccessToken } from '../api/axios';
  ```
- EÄer import farklÄ± bir path'ten yapÄ±lÄ±yorsa (Ã¶rn. `../utils/auth`), o dosyanÄ±n var olup olmadÄ±ÄÄ±nÄ± kontrol et. Yoksa import'u `../api/axios` olarak dÃ¼zelt.

---

### AdÄ±m 4: `DashboardReservations.jsx` DÃ¼zeltmesi

DosyayÄ± okuduktan sonra:

- `initialState` deÄiÅkeninin kullanÄ±ldÄ±ÄÄ± ama tanÄ±mlanmadÄ±ÄÄ± yeri bul.
- DosyanÄ±n en Ã¼st kÄ±smÄ±na (import'lardan hemen sonra, component tanÄ±mÄ±ndan Ã¶nce) Åunu ekle:

```javascript
const initialState = {
  reservations: [],
  loading: false,
  error: null,
  filters: {
    status: 'all',
    dateRange: null,
    search: '',
  },
  pagination: {
    page: 1,
    limit: 10,
    total: 0,
  },
};
```

- EÄer `useReducer` kullanÄ±lÄ±yorsa ve `reducer` fonksiyonu da eksikse, onu da ekle. DosyayÄ± okuyunca anlarsÄ±n.
- EÄer sadece `useState` kullanÄ±lÄ±yorsa ve `initialState` bir obje olarak kullanÄ±lÄ±yorsa, yukarÄ±daki tanÄ±mÄ± dosyaya ekle.

---

### AdÄ±m 5: DeÄiÅiklikleri Kaydet

Her 3 dosyayÄ± da dÃ¼zenledikten sonra kaydet. Dosya kodlamasÄ± UTF-8 olmalÄ±.

---

## Etkilenen Dosyalar
- `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js` â `refreshAccessToken` export ekleme
- `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/context/AuthContext.jsx` â import dÃ¼zeltme
- `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx` â `initialState` tanÄ±mlama

## Test SenaryolarÄ±
- `axios.js` dosyasÄ±nda `refreshAccessToken` artÄ±k export edilmeli
- `AuthContext.jsx` import hatasÄ± (undefined reference) Ã§Ã¶zÃ¼lmeli
- `DashboardReservations.jsx` component render sÄ±rasÄ±nda `initialState is not defined` hatasÄ± vermemeli

## Riskler
- `axios.js` interceptor'Ä± deÄiÅtirmek token yenileme dÃ¶ngÃ¼sÃ¼ne yol aÃ§abilir â mevcut interceptor'Ä± okumadan dokunma
- `DashboardReservations.jsx`'deki `initialState` yapÄ±sÄ± gerÃ§ek API response formatÄ±na uygun olmalÄ±

<commit_message>fix: refreshAccessToken export, initialState tanÄ±mÄ± ve AuthContext import hatasÄ± dÃ¼zeltildi</commit_message>


# FAZ 2 â Kritik HatalarÄ±n DÃ¼zeltilmesi

## AmaÃ§
Analiz fazÄ±nda tespit edilen 3 kritik hatayÄ± dÃ¼zeltmek:
1. `axios.js` dosyasÄ±nda `refreshAccessToken` fonksiyonunun eksik olmasÄ±
2. `DashboardReservations.jsx` dosyasÄ±nda `initialState` tanÄ±msÄ±zlÄ±ÄÄ±
3. `AuthContext.jsx` iÃ§indeki import hatasÄ±

## Executor'a Talimatlar

AÅaÄÄ±daki dosyalarÄ± sÄ±rasÄ±yla oku, analiz et ve dÃ¼zelt.

---

### AdÄ±m 1: DosyalarÄ± Oku

Ãnce Åu dosyalarÄ±n tam iÃ§eriÄini oku:

1. `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js`
2. `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/context/AuthContext.jsx`
3. `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx`

---

### AdÄ±m 2: `axios.js` DÃ¼zeltmesi

DosyayÄ± okuduktan sonra ÅunlarÄ± kontrol et ve dÃ¼zelt:

- EÄer `refreshAccessToken` adÄ±nda bir export edilen fonksiyon **yoksa**, dosyanÄ±n en altÄ±na Åunu ekle (mevcut axios instance yapÄ±sÄ±nÄ± bozmadan):

```javascript
export const refreshAccessToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token');
  
  const response = await axios.post('/auth/refresh', { refreshToken });
  const { accessToken } = response.data;
  localStorage.setItem('accessToken', accessToken);
  return accessToken;
};
```

- EÄer fonksiyon zaten varsa ama export edilmiyorsa, `export` keyword'Ã¼nÃ¼ ekle.
- Mevcut interceptor'lar varsa bozma, sadece eksik olan kÄ±smÄ± ekle.

---

### AdÄ±m 3: `AuthContext.jsx` DÃ¼zeltmesi

DosyayÄ± okuduktan sonra:

- `refreshAccessToken`'Ä±n nereden import edildiÄine bak.
- EÄer `import { refreshAccessToken } from '../api/axios'` Åeklinde import varsa ama axios.js'de bu export yoktu, artÄ±k eklendiÄi iÃ§in sorun Ã§Ã¶zÃ¼lmÃ¼Å olacak.
- EÄer import satÄ±rÄ± hiÃ§ yoksa, dosyanÄ±n Ã¼st kÄ±smÄ±ndaki import bloÄuna Åunu ekle:
  ```javascript
  import { refreshAccessToken } from '../api/axios';
  ```
- EÄer import farklÄ± bir path'ten yapÄ±lÄ±yorsa (Ã¶rn. `../utils/auth`), o dosyanÄ±n var olup olmadÄ±ÄÄ±nÄ± kontrol et. Yoksa import'u `../api/axios` olarak dÃ¼zelt.

---

### AdÄ±m 4: `DashboardReservations.jsx` DÃ¼zeltmesi

DosyayÄ± okuduktan sonra:

- `initialState` deÄiÅkeninin kullanÄ±ldÄ±ÄÄ± ama tanÄ±mlanmadÄ±ÄÄ± yeri bul.
- DosyanÄ±n en Ã¼st kÄ±smÄ±na (import'lardan hemen sonra, component tanÄ±mÄ±ndan Ã¶nce) Åunu ekle:

```javascript
const initialState = {
  reservations: [],
  loading: false,
  error: null,
  filters: {
    status: 'all',
    dateRange: null,
    search: '',
  },
  pagination: {
    page: 1,
    limit: 10,
    total: 0,
  },
};
```

- EÄer `useReducer` kullanÄ±lÄ±yorsa ve `reducer` fonksiyonu da eksikse, onu da ekle. DosyayÄ± okuyunca anlarsÄ±n.
- EÄer sadece `useState` kullanÄ±lÄ±yorsa ve `initialState` bir obje olarak kullanÄ±lÄ±yorsa, yukarÄ±daki tanÄ±mÄ± dosyaya ekle.

---

### AdÄ±m 5: DeÄiÅiklikleri Kaydet

Her 3 dosyayÄ± da dÃ¼zenledikten sonra kaydet. Dosya kodlamasÄ± UTF-8 olmalÄ±.

---

## Etkilenen Dosyalar
- `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js` â `refreshAccessToken` export ekleme
- `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/context/AuthContext.jsx` â import dÃ¼zeltme
- `/c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx` â `initialState` tanÄ±mlama

## Test SenaryolarÄ±
- `axios.js` dosyasÄ±nda `refreshAccessToken` artÄ±k export edilmeli
- `AuthContext.jsx` import hatasÄ± (undefined reference) Ã§Ã¶zÃ¼lmeli
- `DashboardReservations.jsx` component render sÄ±rasÄ±nda `initialState is not defined` hatasÄ± vermemeli

## Riskler
- `axios.js` interceptor'Ä± deÄiÅtirmek token yenileme dÃ¶ngÃ¼sÃ¼ne yol aÃ§abilir â mevcut interceptor'Ä± okumadan dokunma
- `DashboardReservations.jsx`'deki `initialState` yapÄ±sÄ± gerÃ§ek API response formatÄ±na uygun olmalÄ±

<commit_message>fix: refreshAccessToken export, initialState tanÄ±mÄ± ve AuthContext import hatasÄ± dÃ¼zeltildi</commit_message>


Final response requirements:
- Briefly list the files changed.
- Mention any remaining issue or follow-up work.
- Show the git commit message used.
