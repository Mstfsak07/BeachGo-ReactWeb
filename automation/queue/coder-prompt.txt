You have access to a browser via the BrowserMCP tool. 
After making code changes:
1. Use browser_navigate to open http://localhost:3000
2. Check the page visually for errors
3. Test the specific feature you just implemented
4. Report what you saw in the browser

Task:

# FAZ 1: Authentication Backend - Email DoÄrulama ve Åifre SÄ±fÄ±rlama

## SCOPE: api

---

## AdÄ±m 1: Mevcut DosyalarÄ± Oku

Ãnce mevcut durumu anlamak iÃ§in Åu dosyalarÄ± oku:

```
find /workspaces -name "AuthController.cs" -o -name "AuthService.cs" -o -name "IAuthService.cs" -o -name "AuthModels.cs" -o -name "VerificationCode.cs" -o -name "OtpService.cs" -o -name "IOtpService.cs" -o -name "MockSmsService.cs" 2>/dev/null | head -20
```

Sonra API projesinin root'unu bul:

```
find /workspaces -name "*.csproj" | grep -i api | head -5
```

---

## AdÄ±m 2: Dosya DeÄiÅiklikleri

### 2.1 `/[API_ROOT]/Models/AuthModels.cs` - DTO'larÄ± gÃ¼ncelle

Mevcut dosyayÄ± oku, ardÄ±ndan Åu DTO'larÄ±n var olduÄunu ve aÅaÄÄ±daki validasyona sahip olduÄunu doÄrula/ekle:

```csharp
public class ForgotPasswordRequest
{
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
}

public class ResetPasswordRequest
{
    [Required]
    public string Token { get; set; } = string.Empty;
    
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MinLength(6)]
    public string NewPassword { get; set; } = string.Empty;
    
    [Required]
    [Compare("NewPassword")]
    public string ConfirmPassword { get; set; } = string.Empty;
}

public class VerifyEmailRequest
{
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    public string Token { get; set; } = string.Empty;
}

public class ResendVerificationRequest
{
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
}
```

---

### 2.2 `/[API_ROOT]/Services/Interfaces/IAuthService.cs` - Interface metodlarÄ±nÄ± ekle

Åu metodlarÄ±n interface'de tanÄ±mlÄ± olduÄunu doÄrula/ekle:

```csharp
Task<AuthResult> VerifyEmailAsync(string email, string token);
Task<AuthResult> ForgotPasswordAsync(string email);
Task<AuthResult> ResetPasswordAsync(string email, string token, string newPassword);
Task<AuthResult> ResendVerificationAsync(string email);
```

---

### 2.3 `/[API_ROOT]/Services/AuthService.cs` - ImplementasyonlarÄ± ekle

Eksik metodlarÄ± implement et:

```csharp
public async Task<AuthResult> ForgotPasswordAsync(string email)
{
    // User'Ä± email ile bul
    // Bulunamazsa: return AuthResult.Failure("User not found") veya gÃ¼venlik iÃ§in success dÃ¶ndÃ¼r
    // Bulunursa: reset token Ã¼ret (Guid.NewGuid().ToString() veya OtpService kullan)
    // Token'Ä± DB'ye kaydet (expiry: DateTime.UtcNow.AddHours(1))
    // MockSmsService veya email service ile gÃ¶nder (log'a yaz)
    // return AuthResult.Success()
}

public async Task<AuthResult> ResetPasswordAsync(string email, string token, string newPassword)
{
    // User'Ä± email ile bul
    // Reset token'Ä± DB'de doÄrula (expiry kontrolÃ¼ dahil)
    // GeÃ§ersizse: return AuthResult.Failure("Invalid or expired token")
    // GeÃ§erliyse: password hash'le ve gÃ¼ncelle
    // Token'Ä± kullanÄ±ldÄ± olarak iÅaretle / sil
    // return AuthResult.Success()
}

public async Task<AuthResult> VerifyEmailAsync(string email, string token)
{
    // User'Ä± email ile bul
    // Email verification token'Ä±nÄ± kontrol et
    // GeÃ§erliyse: EmailConfirmed = true yap, token'Ä± temizle
    // return AuthResult.Success() veya Failure
}

public async Task<AuthResult> ResendVerificationAsync(string email)
{
    // User'Ä± bul
    // Zaten verify edilmiÅse: return Failure("Already verified")
    // Yeni token Ã¼ret, kaydet, gÃ¶nder
    // return AuthResult.Success()
}
```

---

### 2.4 `/[API_ROOT]/Controllers/AuthController.cs` - Endpoint'leri ekle/dÃ¼zelt

```csharp
[HttpPost("forgot-password")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.ForgotPasswordAsync(request.Email);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "If the email exists, a reset link has been sent." });
}

[HttpPost("reset-password")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.ResetPasswordAsync(request.Email, request.Token, request.NewPassword);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "Password has been reset successfully." });
}

[HttpPost("verify-email")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> VerifyEmail([FromBody] VerifyEmailRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.VerifyEmailAsync(request.Email, request.Token);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "Email verified successfully." });
}

[HttpPost("resend-verification")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> ResendVerification([FromBody] ResendVerificationRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.ResendVerificationAsync(request.Email);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "Verification email resent." });
}
```

---

### 2.5 `/[API_ROOT]/Services/OtpService.cs` - Expiry kontrolÃ¼nÃ¼ doÄrula

```csharp
// GenerateCode: 6 haneli random kod Ã¼ret, SHA256 hash ile sakla, expiry = DateTime.UtcNow.AddMinutes(10)
// ValidateCode: hash karÅÄ±laÅtÄ±r + DateTime.UtcNow < expiry kontrolÃ¼
// IsExpired property veya metodu VerificationCode modelinde olmalÄ±
```

---

### 2.6 `/[API_ROOT]/Models/VerificationCode.cs` - Expiry alanÄ±nÄ± doÄrula

```csharp
public class VerificationCode
{
    public int Id { get; set; }
    public string Email { get; set; } = string.Empty;
    public string CodeHash { get; set; } = string.Empty;
    public DateTime ExpiresAt { get; set; }
    public bool IsUsed { get; set; }
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    
    public bool IsExpired => DateTime.UtcNow > ExpiresAt;
    public bool IsValid => !IsUsed && !IsExpired;
}
```

---

## AdÄ±m 3: Build

API projesinin dizinine git ve build al:

```bash
cd /[API_ROOT] && dotnet build
```

**Build baÅarÄ±sÄ±z olursa:** Hata mesajlarÄ±nÄ± analiz et, tip uyuÅmazlÄ±klarÄ± veya missing using direktiflerini dÃ¼zelt, tekrar build al.

---

## AdÄ±m 4: Test

```bash
# API'yi baÅlat (arka planda)
cd /[API_ROOT] && dotnet run &
sleep 5

# forgot-password endpoint testi
curl -X POST http://localhost:5000/api/auth/forgot-password \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}' \
  -w "\nHTTP Status: %{http_code}\n"

# verify-email endpoint testi  
curl -X POST http://localhost:5000/api/auth/verify-email \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","token":"invalid-token"}' \
  -w "\nHTTP Status: %{http_code}\n"

# reset-password endpoint testi
curl -X POST http://localhost:5000/api/auth/reset-password \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","token":"invalid","newPassword":"Test123!","confirmPassword":"Test123!"}' \
  -w "\nHTTP Status: %{http_code}\n"
```

**Beklenen sonuÃ§lar:**
- `forgot-password`: HTTP 200 (email yoksa da gÃ¼venlik iÃ§in 200)
- `verify-email` invalid token: HTTP 400
- `reset-password` invalid token: HTTP 400

---

<commit_message>feat: implement forgot-password, reset-password, verify-email, resend-verification endpoints with token validation and expiry control</commit_message>


# FAZ 1: Authentication Backend - Email DoÄrulama ve Åifre SÄ±fÄ±rlama

## SCOPE: api

---

## AdÄ±m 1: Mevcut DosyalarÄ± Oku

Ãnce mevcut durumu anlamak iÃ§in Åu dosyalarÄ± oku:

```
find /workspaces -name "AuthController.cs" -o -name "AuthService.cs" -o -name "IAuthService.cs" -o -name "AuthModels.cs" -o -name "VerificationCode.cs" -o -name "OtpService.cs" -o -name "IOtpService.cs" -o -name "MockSmsService.cs" 2>/dev/null | head -20
```

Sonra API projesinin root'unu bul:

```
find /workspaces -name "*.csproj" | grep -i api | head -5
```

---

## AdÄ±m 2: Dosya DeÄiÅiklikleri

### 2.1 `/[API_ROOT]/Models/AuthModels.cs` - DTO'larÄ± gÃ¼ncelle

Mevcut dosyayÄ± oku, ardÄ±ndan Åu DTO'larÄ±n var olduÄunu ve aÅaÄÄ±daki validasyona sahip olduÄunu doÄrula/ekle:

```csharp
public class ForgotPasswordRequest
{
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
}

public class ResetPasswordRequest
{
    [Required]
    public string Token { get; set; } = string.Empty;
    
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MinLength(6)]
    public string NewPassword { get; set; } = string.Empty;
    
    [Required]
    [Compare("NewPassword")]
    public string ConfirmPassword { get; set; } = string.Empty;
}

public class VerifyEmailRequest
{
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    public string Token { get; set; } = string.Empty;
}

public class ResendVerificationRequest
{
    [Required]
    [EmailAddress]
    public string Email { get; set; } = string.Empty;
}
```

---

### 2.2 `/[API_ROOT]/Services/Interfaces/IAuthService.cs` - Interface metodlarÄ±nÄ± ekle

Åu metodlarÄ±n interface'de tanÄ±mlÄ± olduÄunu doÄrula/ekle:

```csharp
Task<AuthResult> VerifyEmailAsync(string email, string token);
Task<AuthResult> ForgotPasswordAsync(string email);
Task<AuthResult> ResetPasswordAsync(string email, string token, string newPassword);
Task<AuthResult> ResendVerificationAsync(string email);
```

---

### 2.3 `/[API_ROOT]/Services/AuthService.cs` - ImplementasyonlarÄ± ekle

Eksik metodlarÄ± implement et:

```csharp
public async Task<AuthResult> ForgotPasswordAsync(string email)
{
    // User'Ä± email ile bul
    // Bulunamazsa: return AuthResult.Failure("User not found") veya gÃ¼venlik iÃ§in success dÃ¶ndÃ¼r
    // Bulunursa: reset token Ã¼ret (Guid.NewGuid().ToString() veya OtpService kullan)
    // Token'Ä± DB'ye kaydet (expiry: DateTime.UtcNow.AddHours(1))
    // MockSmsService veya email service ile gÃ¶nder (log'a yaz)
    // return AuthResult.Success()
}

public async Task<AuthResult> ResetPasswordAsync(string email, string token, string newPassword)
{
    // User'Ä± email ile bul
    // Reset token'Ä± DB'de doÄrula (expiry kontrolÃ¼ dahil)
    // GeÃ§ersizse: return AuthResult.Failure("Invalid or expired token")
    // GeÃ§erliyse: password hash'le ve gÃ¼ncelle
    // Token'Ä± kullanÄ±ldÄ± olarak iÅaretle / sil
    // return AuthResult.Success()
}

public async Task<AuthResult> VerifyEmailAsync(string email, string token)
{
    // User'Ä± email ile bul
    // Email verification token'Ä±nÄ± kontrol et
    // GeÃ§erliyse: EmailConfirmed = true yap, token'Ä± temizle
    // return AuthResult.Success() veya Failure
}

public async Task<AuthResult> ResendVerificationAsync(string email)
{
    // User'Ä± bul
    // Zaten verify edilmiÅse: return Failure("Already verified")
    // Yeni token Ã¼ret, kaydet, gÃ¶nder
    // return AuthResult.Success()
}
```

---

### 2.4 `/[API_ROOT]/Controllers/AuthController.cs` - Endpoint'leri ekle/dÃ¼zelt

```csharp
[HttpPost("forgot-password")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.ForgotPasswordAsync(request.Email);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "If the email exists, a reset link has been sent." });
}

[HttpPost("reset-password")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.ResetPasswordAsync(request.Email, request.Token, request.NewPassword);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "Password has been reset successfully." });
}

[HttpPost("verify-email")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> VerifyEmail([FromBody] VerifyEmailRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.VerifyEmailAsync(request.Email, request.Token);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "Email verified successfully." });
}

[HttpPost("resend-verification")]
[AllowAnonymous]
[ProducesResponseType(200)]
[ProducesResponseType(400)]
public async Task<IActionResult> ResendVerification([FromBody] ResendVerificationRequest request)
{
    if (!ModelState.IsValid)
        return BadRequest(ModelState);
    
    var result = await _authService.ResendVerificationAsync(request.Email);
    if (!result.Success)
        return BadRequest(new { message = result.Error });
    
    return Ok(new { message = "Verification email resent." });
}
```

---

### 2.5 `/[API_ROOT]/Services/OtpService.cs` - Expiry kontrolÃ¼nÃ¼ doÄrula

```csharp
// GenerateCode: 6 haneli random kod Ã¼ret, SHA256 hash ile sakla, expiry = DateTime.UtcNow.AddMinutes(10)
// ValidateCode: hash karÅÄ±laÅtÄ±r + DateTime.UtcNow < expiry kontrolÃ¼
// IsExpired property veya metodu VerificationCode modelinde olmalÄ±
```

---

### 2.6 `/[API_ROOT]/Models/VerificationCode.cs` - Expiry alanÄ±nÄ± doÄrula

```csharp
public class VerificationCode
{
    public int Id { get; set; }
    public string Email { get; set; } = string.Empty;
    public string CodeHash { get; set; } = string.Empty;
    public DateTime ExpiresAt { get; set; }
    public bool IsUsed { get; set; }
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    
    public bool IsExpired => DateTime.UtcNow > ExpiresAt;
    public bool IsValid => !IsUsed && !IsExpired;
}
```

---

## AdÄ±m 3: Build

API projesinin dizinine git ve build al:

```bash
cd /[API_ROOT] && dotnet build
```

**Build baÅarÄ±sÄ±z olursa:** Hata mesajlarÄ±nÄ± analiz et, tip uyuÅmazlÄ±klarÄ± veya missing using direktiflerini dÃ¼zelt, tekrar build al.

---

## AdÄ±m 4: Test

```bash
# API'yi baÅlat (arka planda)
cd /[API_ROOT] && dotnet run &
sleep 5

# forgot-password endpoint testi
curl -X POST http://localhost:5000/api/auth/forgot-password \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}' \
  -w "\nHTTP Status: %{http_code}\n"

# verify-email endpoint testi  
curl -X POST http://localhost:5000/api/auth/verify-email \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","token":"invalid-token"}' \
  -w "\nHTTP Status: %{http_code}\n"

# reset-password endpoint testi
curl -X POST http://localhost:5000/api/auth/reset-password \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","token":"invalid","newPassword":"Test123!","confirmPassword":"Test123!"}' \
  -w "\nHTTP Status: %{http_code}\n"
```

**Beklenen sonuÃ§lar:**
- `forgot-password`: HTTP 200 (email yoksa da gÃ¼venlik iÃ§in 200)
- `verify-email` invalid token: HTTP 400
- `reset-password` invalid token: HTTP 400

---

<commit_message>feat: implement forgot-password, reset-password, verify-email, resend-verification endpoints with token validation and expiry control</commit_message>


Final response requirements:
- Briefly list the files changed.
- Mention any remaining issue or follow-up work.
- Show the git commit message used.
