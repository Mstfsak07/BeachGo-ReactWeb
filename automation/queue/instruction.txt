- `ONCELIK: FEATURE`
- `SCOPE: api`
- `DOSYALAR: src/BeachGo.API/Middleware/GlobalExceptionMiddleware.cs, src/BeachGo.API/Models/ApiResponse.cs`
- `GOREV:`
  1. `ApiResponse<T>` sınıfı: `success`, `message`, `data`, `errors` alanları
  2. `GlobalExceptionMiddleware`'de `ValidationException` → 400, `NotFoundException` → 404, `UnauthorizedAccessException` → 401, diğer → 500
  3. Her durumda JSON olarak `ApiResponse` dön
  4. `Program.cs`'de middleware'i `app.UseMiddleware<GlobalExceptionMiddleware>()` ile kaydet
- `BUILD: dotnet build src/BeachGo.API/BeachGo.API.csproj`
- `TEST:`
  1. Geçersiz token ile istek → `{"success":false,"message":"..."}` ve HTTP 401
  2. Olmayan ID ile GET → HTTP 404 ve `success:false`
  3. Eksik zorunlu alan ile POST → HTTP 400 ve `errors` dizisi dolu
- `<commit_message>feat: add global exception middleware and ApiResponse format</commit_message>`


