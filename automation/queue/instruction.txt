# FAZ 1 - Authentication Backend Tamamlama

## Proje YapÄ±sÄ±nÄ± Ãnce KeÅfet

AÅaÄÄ±daki adÄ±mlarÄ± sÄ±rayla uygula:

### ADIM 1: Mevcut DosyalarÄ± Bul

```bash
find /workspace -name "*.cs" | grep -E "(Auth|Otp|Verification|Sms)" | head -30
find /workspace -name "*.csproj" | head -5
```

### ADIM 2: API Projesinin Yolunu Belirle ve DosyalarÄ± Oku

```bash
# .csproj dosyasÄ±nÄ±n bulunduÄu dizine git
# Åu dosyalarÄ± oku (varsa):
# - Controllers/AuthController.cs
# - Services/AuthService.cs
# - Services/IAuthService.cs
# - Models/AuthModels.cs
# - Models/VerificationCode.cs
# - Services/OtpService.cs
# - Services/IOtpService.cs
# - Services/MockSmsService.cs
```

### ADIM 3: Eksik Endpoint'leri ve MetodlarÄ± Tamamla

**`IAuthService.cs`** â Åu metodlarÄ±n interface'de tanÄ±mlÄ± olduÄundan emin ol:
```csharp
Task<AuthResult> ForgotPasswordAsync(string email);
Task<AuthResult> ResetPasswordAsync(string token, string newPassword);
Task<AuthResult> VerifyEmailAsync(string token);
Task<AuthResult> ResendVerificationAsync(string email);
```

**`AuthService.cs`** â YukarÄ±daki metodlarÄ±n implementasyonunu ekle:
- `ForgotPasswordAsync`: KullanÄ±cÄ±yÄ± email ile bul, reset token Ã¼ret (Guid veya random string), token'Ä± DB'ye kaydet (expiry: 1 saat), MockSmsService veya email ile gÃ¶nder
- `ResetPasswordAsync`: Token'Ä± DB'de bul, expiry kontrol et, Åifreyi hash'le ve gÃ¼ncelle, token'Ä± geÃ§ersiz kÄ±l
- `VerifyEmailAsync`: Token ile VerificationCode kaydÄ±nÄ± bul, expiry kontrol et, User.EmailVerified = true yap
- `ResendVerificationAsync`: KullanÄ±cÄ±yÄ± bul, yeni token Ã¼ret, eski token'larÄ± geÃ§ersiz kÄ±l, yeni token gÃ¶nder

**`AuthController.cs`** â Åu endpoint'lerin tam olarak var olduÄunu doÄrula ve ekle:
```csharp
[HttpPost("forgot-password")]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)

[HttpPost("reset-password")]  
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)

[HttpGet("verify-email")]  // veya [HttpPost]
public async Task<IActionResult> VerifyEmail([FromQuery] string token) // veya [FromBody]

[HttpPost("resend-verification")]
public async Task<IActionResult> ResendVerification([FromBody] ResendVerificationRequest request)
```

**`AuthModels.cs`** â DTO'larÄ±n validasyonlu olduÄundan emin ol:
```csharp
public class ForgotPasswordRequest {
    [Required, EmailAddress]
    public string Email { get; set; }
}

public class ResetPasswordRequest {
    [Required]
    public string Token { get; set; }
    [Required, MinLength(6)]
    public string NewPassword { get; set; }
    [Required, Compare("NewPassword")]
    public string ConfirmPassword { get; set; }
}

public class VerifyEmailRequest {
    [Required]
    public string Token { get; set; }
}

public class ResendVerificationRequest {
    [Required, EmailAddress]
    public string Email { get; set; }
}
```

**`OtpService.cs`** â Kod Ã¼retme ve doÄrulamayÄ± kontrol et:
- `GenerateCode()`: 6 haneli random sayÄ±, expiry 10 dakika
- `VerifyCode(string email, string code)`: Kodu bul, expiry kontrol et, IsUsed kontrolÃ¼ yap, baÅarÄ±lÄ± ise IsUsed = true yap

**`VerificationCode.cs`** â Entity'de Åu alanlarÄ±n olduÄundan emin ol:
```csharp
public string Token { get; set; }
public string Email { get; set; }
public string Code { get; set; }
public DateTime ExpiresAt { get; set; }
public bool IsUsed { get; set; }
public string Type { get; set; } // "email_verify" | "password_reset"
```

### ADIM 4: Build Et

```bash
cd [API_PROJE_DIZINI]
dotnet build
```

Build hatasÄ± varsa hatalarÄ± oku ve dÃ¼zelt. Sonra tekrar build et.

### ADIM 5: Sonucu result.txt'e Yaz

Build Ã§Ä±ktÄ±sÄ±nÄ± ve hangi dosyalarÄ±n deÄiÅtirildiÄini `/workspace/result.txt` dosyasÄ±na yaz:
```
BUILD: SUCCESS veya FAILED
HATALAR: [varsa listele]
DEGISTIRILEN_DOSYALAR: [liste]
TEST_NOTLARI: [endpoint'ler mevcut mu]
```

---

<commit_message>feat: complete auth backend - forgot-password, reset-password, verify-email, resend-verification endpoints with OTP validation</commit_message>
