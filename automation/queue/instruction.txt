Ãnceki sonuÃ§ BASARILI ve Build 0 hata ile tamamlanmÄ±Å. Faz 1 baÅarÄ±yla tamamlandÄ±.

Phases.txt'ten sÄ±radaki faza geÃ§iyorum.

---

**FAZ 2 â INSTRUCTION**

**SCOPE:** api

**Hedef:** JWT token Ã¼retimi, refresh token mekanizmasÄ± ve kullanÄ±cÄ± oturum yÃ¶netimini tamamla.

**DeÄiÅtirilecek/OluÅturulacak Dosyalar:**

1. `/app/api/Services/TokenService.cs` (yeni veya mevcut)
2. `/app/api/Services/ITokenService.cs` (yeni veya mevcut)
3. `/app/api/Controllers/AuthController.cs`
4. `/app/api/Models/AuthModels.cs`
5. `/app/api/Models/RefreshToken.cs` (yeni veya mevcut)

**YapÄ±lacaklar:**

**ITokenService.cs:**
```csharp
string GenerateAccessToken(ApplicationUser user);
string GenerateRefreshToken();
Task<bool> ValidateRefreshTokenAsync(string userId, string refreshToken);
Task RevokeRefreshTokenAsync(string userId, string refreshToken);
Task SaveRefreshTokenAsync(string userId, string refreshToken, DateTime expiry);
ClaimsPrincipal? GetPrincipalFromExpiredToken(string token);
```

**TokenService.cs:**
- `GenerateAccessToken`: JWT Ã¼ret, claims iÃ§ine `userId`, `email`, `role` ekle, expiry 15 dakika
- `GenerateRefreshToken`: `RandomNumberGenerator` ile 64-byte base64 string Ã¼ret
- `SaveRefreshTokenAsync`: RefreshToken entity'sini DB'ye kaydet (userId, token hash, expiry, isRevoked)
- `ValidateRefreshTokenAsync`: DB'de token'Ä± bul, sÃ¼resi dolmamÄ±Å ve revoke edilmemiÅ olduÄunu doÄrula
- `RevokeRefreshTokenAsync`: Token'Ä± DB'de revoke et (isRevoked = true)
- `GetPrincipalFromExpiredToken`: SÃ¼resi dolmuÅ token'dan ClaimsPrincipal Ã§Ä±kar, imzayÄ± doÄrula

**RefreshToken.cs (Model/Entity):**
```csharp
public class RefreshToken {
    public int Id { get; set; }
    public string UserId { get; set; }
    public string Token { get; set; } // SHA256 hash olarak sakla
    public DateTime ExpiresAt { get; set; }
    public bool IsRevoked { get; set; }
    public DateTime CreatedAt { get; set; }
}
```

**AuthController.cs â Yeni Endpoint Ekle:**
- `POST /api/auth/refresh-token`: Body'den `{ accessToken, refreshToken }` al â `GetPrincipalFromExpiredToken` ile userId Ã§Ä±kar â `ValidateRefreshTokenAsync` â eski token'Ä± revoke et â yeni access + refresh token Ã¼ret â dÃ¶ndÃ¼r
- `POST /api/auth/revoke-token`: `[Authorize]` â mevcut kullanÄ±cÄ±nÄ±n refresh token'Ä±nÄ± iptal et
- Login endpoint'inden dÃ¶nen response'a `refreshToken` alanÄ±nÄ± ekle

**AuthModels.cs â DTO ekle:**
```csharp
public class RefreshTokenRequest {
    [Required] public string AccessToken { get; set; }
    [Required] public string RefreshToken { get; set; }
}
public class TokenResponse {
    public string AccessToken { get; set; }
    public string RefreshToken { get; set; }
    public DateTime ExpiresAt { get; set; }
}
```

**Program.cs / DI:**
- `ITokenService` â `TokenService` olarak register et (Scoped)
- `RefreshToken` entity'sini DbContext'e ekle (eÄer EF kullanÄ±lÄ±yorsa)
- Migration Ã§alÄ±ÅtÄ±r: `dotnet ef migrations add AddRefreshTokenTable` ve `dotnet ef database update`

---

**Build & Test:**

```bash
cd /app/api
dotnet build
```

Build 0 hata ile tamamlanmalÄ±.

**Swagger Testleri:**
1. `POST /api/auth/login` â response iÃ§inde `refreshToken` alanÄ± bulunmalÄ±
2. `POST /api/auth/refresh-token` â geÃ§erli token Ã§ifti ile 200 + yeni token Ã§ifti dÃ¶nmeli
3. `POST /api/auth/refresh-token` â geÃ§ersiz/sÃ¼resi dolmuÅ token ile 401 dÃ¶nmeli
4. `POST /api/auth/revoke-token` â Bearer token ile 200 dÃ¶nmeli

---

<commit_message>feat: JWT refresh token mechanism with revocation support added</commit_message>
