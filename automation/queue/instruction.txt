# FAZ 1 - Authentication Backend Tamamlama

## Proje YapÄ±sÄ±nÄ± Ãnce KeÅfet

AÅaÄÄ±daki adÄ±mlarÄ± sÄ±rayla uygula:

### ADIM 1: Mevcut dosyalarÄ± bul ve oku

```bash
find /workspace -type f -name "*.cs" | grep -E "(Auth|Otp|Verification|MockSms)" | head -30
find /workspace -type f -name "*.csproj" | head -5
```

Proje kÃ¶k dizinini belirle, sonra aÅaÄÄ±daki dosyalarÄ± oku:
- `AuthController.cs` (tam path'i bul)
- `AuthService.cs`
- `IAuthService.cs`  
- `AuthModels.cs`
- `OtpService.cs` veya `IOtpService.cs`
- `VerificationCode.cs`

### ADIM 2: Eksik metodlarÄ± tespit et ve uygula

**IAuthService.cs** iÃ§inde Åu metodlarÄ±n var olduÄunu doÄrula, yoksa ekle:
```csharp
Task<bool> ForgotPasswordAsync(string email);
Task<bool> ResetPasswordAsync(string token, string newPassword);
Task<bool> VerifyEmailAsync(string token);
Task<bool> ResendVerificationAsync(string email);
```

**AuthService.cs** iÃ§inde bu metodlarÄ±n implementasyonunu kontrol et. Eksikse ekle:
- `ForgotPasswordAsync`: KullanÄ±cÄ±yÄ± email ile bul, reset token oluÅtur (Guid veya OTP), kaydet, (mock) gÃ¶nder
- `ResetPasswordAsync`: Token'Ä± doÄrula, sÃ¼resi geÃ§miÅse 400, yeni Åifreyi hash'le, kaydet
- `VerifyEmailAsync`: Token ile kullanÄ±cÄ±yÄ± bul, `EmailVerified = true` yap, kaydet
- `ResendVerificationAsync`: KullanÄ±cÄ±yÄ± bul, yeni token Ã¼ret, (mock) gÃ¶nder

**AuthController.cs** iÃ§inde Åu endpoint'lerin var olduÄunu doÄrula, yoksa ekle:
```csharp
[HttpPost("forgot-password")]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)

[HttpPost("reset-password")]  
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)

[HttpPost("verify-email")]
public async Task<IActionResult> VerifyEmail([FromBody] VerifyEmailRequest request)

[HttpPost("resend-verification")]
public async Task<IActionResult> ResendVerification([FromBody] ResendVerificationRequest request)
```

**AuthModels.cs** iÃ§inde bu DTO'larÄ± kontrol et, eksikse ekle:
```csharp
public class ForgotPasswordRequest { public string Email { get; set; } }
public class ResetPasswordRequest { public string Token { get; set; } public string NewPassword { get; set; } }
public class VerifyEmailRequest { public string Token { get; set; } }
public class ResendVerificationRequest { public string Email { get; set; } }
```

**OtpService.cs** / **IOtpService.cs** â `GenerateCode()` ve `ValidateCode(string code)` metodlarÄ±nÄ±n expiry kontrolÃ¼ (Ã¶rn. 10 dakika) yaptÄ±ÄÄ±nÄ± doÄrula.

### ADIM 3: Build al

```bash
cd /workspace/<api-proje-dizini>
dotnet build
```

Build Ã§Ä±ktÄ±sÄ±nÄ± result.txt iÃ§ine yaz. Hata varsa dÃ¼zelt ve tekrar build al.

### ADIM 4: result.txt'e yaz

Åunu result.txt'e kaydet:
```
BUILD: SUCCESS veya FAILED
HATALAR: (varsa listele)
EKLENEN METODLAR: (ne eklendiÄini yaz)
ENDPOINT'LER: forgot-password, reset-password, verify-email, resend-verification - MEVCUT/EKLENDÄ°
```

<commit_message>feat: complete auth flow - forgot-password, reset-password, verify-email, resend-verification endpoints implemented</commit_message>
