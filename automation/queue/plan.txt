# FAZ 1 - Authentication Backend - API Scope

## HATA ANALÄ°ZÄ°

GeÃ§ici dosyaya eriÅim sorunu (temp file lock) var. Bu sistem sorunudur, kod deÄiÅikliÄi deÄil. Faz 1'i tekrar Ã§alÄ±ÅtÄ±r.

---

## INSTRUCTION

**SCOPE: api**

**Proje Dizini:** `C:\Users\akMuratNET\Desktop\BeachGo\BeachGo-API`

---

### ADIM 1: Mevcut dosyalarÄ± oku

AÅaÄÄ±daki dosyalarÄ± oku ve iÃ§eriklerini gÃ¶ster:

1. `/Controllers/AuthController.cs`
2. `/Services/AuthService.cs`
3. `/Services/IAuthService.cs`
4. `/Models/AuthModels.cs`
5. `/Models/VerificationCode.cs`
6. `/Services/OtpService.cs`
7. `/Services/IOtpService.cs`

---

### ADIM 2: IAuthService.cs â Eksik metodlarÄ± ekle

`IAuthService` interface'inde aÅaÄÄ±daki metodlarÄ±n **tam olarak** var olduÄunu doÄrula, yoksa ekle:

```csharp
Task<ServiceResult> ForgotPasswordAsync(ForgotPasswordRequest request);
Task<ServiceResult> ResetPasswordAsync(ResetPasswordRequest request);
Task<ServiceResult> VerifyEmailAsync(VerifyEmailRequest request);
Task<ServiceResult> ResendVerificationAsync(ResendVerificationRequest request);
```

---

### ADIM 3: AuthModels.cs â DTO'larÄ± doÄrula ve ekle

AÅaÄÄ±daki DTO'larÄ±n eksiksiz var olduÄunu kontrol et, yoksa ekle:

```csharp
public class ForgotPasswordRequest
{
    public string Email { get; set; }
}

public class ResetPasswordRequest
{
    public string Email { get; set; }
    public string Token { get; set; }
    public string NewPassword { get; set; }
}

public class VerifyEmailRequest
{
    public string Email { get; set; }
    public string Token { get; set; }
}

public class ResendVerificationRequest
{
    public string Email { get; set; }
}
```

---

### ADIM 4: AuthService.cs â MetodlarÄ± implement et

`AuthService` iÃ§inde Åu 4 metodu implement et (yoksa ekle, varsa doÄrula):

**ForgotPasswordAsync:**
- Email ile kullanÄ±cÄ± bul, bulunamazsa `ServiceResult.Fail("User not found")` dÃ¶n
- OtpService ile 6 haneli kod Ã¼ret
- Kodu DB'ye kaydet (VerificationCode tablosu), expiry = 15 dakika
- `ServiceResult.Success("Reset code sent")` dÃ¶n

**ResetPasswordAsync:**
- Email + Token ile VerificationCode tablosundan kaydÄ± bul
- Expiry kontrolÃ¼ yap, sÃ¼resi geÃ§miÅse `Fail("Token expired")` dÃ¶n
- KullanÄ±cÄ±nÄ±n password hash'ini gÃ¼ncelle
- VerificationCode kaydÄ±nÄ± sil
- `ServiceResult.Success("Password reset successful")` dÃ¶n

**VerifyEmailAsync:**
- Email + Token ile VerificationCode kaydÄ±nÄ± bul
- Expiry kontrolÃ¼ yap
- KullanÄ±cÄ±nÄ±n `EmailVerified = true` yap
- VerificationCode kaydÄ±nÄ± sil
- `ServiceResult.Success("Email verified")` dÃ¶n

**ResendVerificationAsync:**
- Email ile kullanÄ±cÄ± bul
- Zaten verified ise `Fail("Email already verified")` dÃ¶n
- Yeni OTP Ã¼ret, kaydet, "Code resent" dÃ¶n

---

### ADIM 5: AuthController.cs â Endpoint'leri ekle/doÄrula

```csharp
[HttpPost("forgot-password")]
public async Task<IActionResult> ForgotPassword([FromBody] ForgotPasswordRequest request)
{
    var result = await _authService.ForgotPasswordAsync(request);
    return result.Success ? Ok(result) : BadRequest(result);
}

[HttpPost("reset-password")]
public async Task<IActionResult> ResetPassword([FromBody] ResetPasswordRequest request)
{
    var result = await _authService.ResetPasswordAsync(request);
    return result.Success ? Ok(result) : BadRequest(result);
}

[HttpPost("verify-email")]
public async Task<IActionResult> VerifyEmail([FromBody] VerifyEmailRequest request)
{
    var result = await _authService.VerifyEmailAsync(request);
    return result.Success ? Ok(result) : BadRequest(result);
}

[HttpPost("resend-verification")]
public async Task<IActionResult> ResendVerification([FromBody] ResendVerificationRequest request)
{
    var result = await _authService.ResendVerificationAsync(request);
    return result.Success ? Ok(result) : BadRequest(result);
}
```

---

### ADIM 6: OtpService.cs â DoÄrula

`OtpService` iÃ§inde ÅunlarÄ±n var olduÄunu kontrol et:
- `GenerateCode()` â 6 haneli random string dÃ¶ner
- `ValidateCode(email, code)` â DB'den bulur, expiry kontrolÃ¼ yapar, bool dÃ¶ner

---

### ADIM 7: Build et

```bash
cd C:\Users\akMuratNET\Desktop\BeachGo\BeachGo-API
dotnet build
```

Build Ã§Ä±ktÄ±sÄ±nÄ± **tam olarak** result.txt'e yaz. Hata varsa hata mesajlarÄ±nÄ± dahil et.

---

### ADIM 8: Test et

Swagger Ã¼zerinden Åu endpoint'leri test et:
- `POST /api/auth/forgot-password` â `{"email":"test@test.com"}` â 200 veya 400 dÃ¶nmeli
- `POST /api/auth/verify-email` â geÃ§ersiz token â 400 dÃ¶nmeli

Test sonuÃ§larÄ±nÄ± result.txt'e yaz.

---

### RESULT FORMAT

result.txt iÃ§ine Åunu yaz:

```
SONUC: BASARILI veya HATALI
BUILD: [dotnet build Ã§Ä±ktÄ±sÄ± - tÃ¼m satÄ±rlar]
EKLENEN_METODLAR: [hangi metodlar eklendi/gÃ¼ncellendi]
TEST: [swagger test sonuÃ§larÄ±]
HATALAR: [varsa hata listesi]
```

---

<commit_message>feat: auth endpoints - forgot-password, reset-password, verify-email, resend-verification implemented</commit_message>
