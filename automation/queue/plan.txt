# FAZ 9 â DoÄrudan Dosya YazÄ±mÄ±: axios.js + DashboardReservations.jsx + Profile.jsx

## AmaÃ§

Executor'Ä±n `write_file` aracÄ± olmadan patch script Ã¼rettiÄi gÃ¶zlemlendi. Bu sefer tÃ¼m dosya iÃ§eriklerini **doÄrudan shell komutlarÄ±yla** yazacak Åekilde instruction hazÄ±rlanacak. Gemini CLI'nin `bash`/`powershell` Ã§alÄ±ÅtÄ±rma yetkisi var, bu yol kullanÄ±lacak.

---

## Executor TalimatlarÄ±

### AdÄ±m 1: Mevcut axios.js Ä°Ã§eriÄini Oku

```
read_file: C:/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/api/axios.js
```

DosyayÄ± okuduktan sonra, eÄer `export const refreshAccessToken` ifadesi **yoksa** AdÄ±m 2'ye geÃ§.

---

### AdÄ±m 2: axios.js'e refreshAccessToken Ekle

DosyanÄ±n **sonuna** aÅaÄÄ±daki bloÄu ekle. Bunu yapmak iÃ§in dosyanÄ±n tÃ¼m mevcut iÃ§eriÄini koru, sadece sona ekle:

Mevcut dosya iÃ§eriÄini al, sonuna Åunu ekle:

```javascript
export const refreshAccessToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token available');
  const response = await api.post('/auth/refresh-token', { refreshToken });
  const { accessToken, refreshToken: newRefreshToken } = response.data;
  localStorage.setItem('token', accessToken);
  if (newRefreshToken) localStorage.setItem('refreshToken', newRefreshToken);
  return accessToken;
};
```

DosyayÄ± kaydet. KayÄ±t sonrasÄ± tekrar oku, `refreshAccessToken` satÄ±rÄ±nÄ±n var olduÄunu doÄrula.

---

### AdÄ±m 3: Profile.jsx DosyasÄ±nÄ± OluÅtur

**Dosya yolu:** `C:/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/Profile.jsx`

Bu dosya **yoksa** aÅaÄÄ±daki iÃ§erikle oluÅtur (dosya varsa iÃ§eriÄini kontrol et, eksikse Ã¼zerine yaz):

```jsx
import React, { useState, useEffect } from 'react';
import { useAuth } from '../context/AuthContext';
import api from '../api/axios';
import toast from 'react-hot-toast';

const Profile = () => {
  const { user, setUser } = useAuth();
  const [loading, setLoading] = useState(false);
  const [form, setForm] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
  });

  useEffect(() => {
    if (user) {
      setForm({
        firstName: user.firstName || '',
        lastName: user.lastName || '',
        email: user.email || '',
        phone: user.phone || '',
      });
    }
  }, [user]);

  const handleChange = (e) => {
    setForm((prev) => ({ ...prev, [e.target.name]: e.target.value }));
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    try {
      const res = await api.put('/auth/profile', form);
      toast.success('Profil gÃ¼ncellendi');
      if (setUser) setUser(res.data);
    } catch (err) {
      toast.error(err?.response?.data?.message || 'GÃ¼ncelleme baÅarÄ±sÄ±z');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="max-w-xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Profilim</h1>
      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label className="block text-sm font-medium mb-1">Ad</label>
          <input
            name="firstName"
            value={form.firstName}
            onChange={handleChange}
            className="w-full border rounded-lg px-3 py-2"
          />
        </div>
        <div>
          <label className="block text-sm font-medium mb-1">Soyad</label>
          <input
            name="lastName"
            value={form.lastName}
            onChange={handleChange}
            className="w-full border rounded-lg px-3 py-2"
          />
        </div>
        <div>
          <label className="block text-sm font-medium mb-1">E-posta</label>
          <input
            name="email"
            type="email"
            value={form.email}
            onChange={handleChange}
            className="w-full border rounded-lg px-3 py-2"
          />
        </div>
        <div>
          <label className="block text-sm font-medium mb-1">Telefon</label>
          <input
            name="phone"
            value={form.phone}
            onChange={handleChange}
            className="w-full border rounded-lg px-3 py-2"
          />
        </div>
        <button
          type="submit"
          disabled={loading}
          className="w-full bg-blue-600 text-white py-2 rounded-lg font-semibold hover:bg-blue-700 disabled:opacity-50"
        >
          {loading ? 'Kaydediliyor...' : 'Kaydet'}
        </button>
      </form>
    </div>
  );
};

export default Profile;
```

DosyayÄ± kaydet. KayÄ±t sonrasÄ± dosyanÄ±n var olduÄunu ve `const Profile` iÃ§erdiÄini doÄrula.

---

### AdÄ±m 4: DashboardReservations.jsx'i Oku ve initialState Sorununu Kontrol Et

```
read_file: C:/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/pages/DashboardReservations.jsx
```

Dosyada Åu iki durumdan birinin olup olmadÄ±ÄÄ±nÄ± kontrol et:

**Sorunlu durum A** â `initialState` kullanÄ±lmadan Ã¶nce tanÄ±mlanmamÄ±Å:
```js
const [filters, setFilters] = useState(prevFilters ?? initialState);
// ... daha sonra:
const initialState = useMemo(() => ({ ... }), []);
```

**Sorunlu durum B** â `useSearchParams` hook'u, diÄer state'lerden Ã¶nce Ã§aÄrÄ±lmÄ±yor.

EÄer bu sorunlar **mevcutsa**, dosyayÄ± Åu kurala gÃ¶re dÃ¼zenle:

1. `initialState` tanÄ±mÄ±nÄ± bul (useMemo veya sabit obje olarak).
2. Bu tanÄ±mÄ± dosyanÄ±n en Ã¼stÃ¼ndeki hook bÃ¶lÃ¼mÃ¼ne (diÄer useState'lerden Ã¶nce) taÅÄ±.
3. `useState(prevFilters ?? initialState)` satÄ±rÄ±nÄ± `useState(initialState)` olarak deÄiÅtir (prevFilters zaten initialState referans eder).

DeÄiÅiklik sonrasÄ± dosyayÄ± kaydet ve tekrar oku: `initialState` tanÄ±mÄ±nÄ±n `useState` Ã§aÄrÄ±larÄ±ndan Ã¶nce geldiÄini doÄrula.

---

### AdÄ±m 5: App.js'e Profile Route Ekle

```
read_file: C:/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui/src/App.js
```

Dosyada `/profile` route'u **yoksa**:

1. Lazy import listesine ekle:
```js
const Profile = lazy(() => import('./pages/Profile'));
```

2. Route listesine (ProtectedRoute iÃ§ine) ekle:
```jsx
<Route path="/profile" element={<ProtectedRoute><Profile /></ProtectedRoute>} />
```

`ProtectedRoute` bileÅeninin adÄ± mevcut App.js yapÄ±sÄ±na gÃ¶re ayarlanmalÄ± (`PrivateRoute` da olabilir â hangisi kullanÄ±lÄ±yorsa onu kullan).

DeÄiÅiklik sonrasÄ± App.js'i tekrar oku: `/profile` route'unun mevcut olduÄunu doÄrula.

---

### AdÄ±m 6: SonuÃ§ DoÄrulama Raporu

Her adÄ±m sonrasÄ± Åu kontrolleri yap ve sonuÃ§larÄ± result.txt'e yaz:

```
DOÄRULAMA:
- axios.js â refreshAccessToken export mevcut: [EVET/HAYIR]
- Profile.jsx â Dosya mevcut ve Profile componenti tanÄ±mlÄ±: [EVET/HAYIR]  
- DashboardReservations.jsx â initialState, useState'ten Ã¶nce: [EVET/HAYIR]
- App.js â /profile route mevcut: [EVET/HAYIR]
```

---

## BaÅarÄ± Kriterleri

- [ ] `axios.js` â `export const refreshAccessToken` satÄ±rÄ± mevcut
- [ ] `Profile.jsx` â dosya oluÅturulmuÅ, form ve handleSubmit var
- [ ] `DashboardReservations.jsx` â `initialState` doÄru sÄ±rada tanÄ±mlanmÄ±Å
- [ ] `App.js` â `/profile` lazy route eklenmiÅ

---

## Notlar

- Executor'Ä±n Ã¶nceki iterasyonda `write_file` aracÄ±na eriÅimi olmadÄ±ÄÄ± gÃ¶zlemlendi. Bu iterasyonda aynÄ± araÃ§ seti geÃ§erli olabilir. Bu durumda executor **dosya iÃ§eriklerini doÄrudan edit/write araÃ§larÄ±yla** yazmalÄ± â patch script Ã¼retmemeli.
- EÄer hiÃ§bir yazma aracÄ± yoksa, executor bunu aÃ§Ä±kÃ§a belirtmeli ve hangi araÃ§larÄ±n mevcut olduÄunu listemelidir.

<commit_message>fix: axios.js refreshAccessToken eklendi, Profile.jsx oluÅturuldu, App.js route gÃ¼ncellendi</commit_message>
