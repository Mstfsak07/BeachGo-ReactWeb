# FAZ 4 â Shell Komutu ile Kritik HatalarÄ±n DÃ¼zeltilmesi

## Durum Analizi

Executor'Ä±n `replace`/`write_file` araÃ§larÄ± Ã§alÄ±ÅmÄ±yor. Ancak history.log'da Iteration 2'de "Commit baÅarÄ±lÄ±" gÃ¶rÃ¼nÃ¼yor â demek ki executor shell komutlarÄ± Ã§alÄ±ÅtÄ±rabiliyor. Bu fazda doÄrudan `bash`/`cmd` ile `sed`, `echo` veya `node -e` komutlarÄ± kullanarak dosyalarÄ± dÃ¼zelteceÄiz.

## Hedef

3 kritik hatayÄ± shell komutlarÄ±yla fiziksel olarak dÃ¼zelt:
1. `axios.js` â `refreshAccessToken` export et
2. `DashboardReservations.jsx` â `initialState` const tanÄ±mla
3. Her ikisini de doÄrula

---

## Executor TalimatlarÄ±

### AdÄ±m 1: Proje KÃ¶k Dizinine Git

```bash
cd /c/Users/akMuratNET/Desktop/BeachGo/BeachGo-ReactWeb/beach-ui
```

---

### AdÄ±m 2: axios.js DosyasÄ±nÄ± Kontrol Et

```bash
cat src/api/axios.js | tail -30
```

`refreshAccessToken` zaten var mÄ± kontrol et. EÄer yoksa AdÄ±m 3'e geÃ§.

---

### AdÄ±m 3: axios.js'e refreshAccessToken Ekle

```bash
node -e "
const fs = require('fs');
const path = 'src/api/axios.js';
let content = fs.readFileSync(path, 'utf8');

if (content.includes('refreshAccessToken')) {
  console.log('refreshAccessToken zaten mevcut, deÄiÅiklik yapÄ±lmadÄ±.');
} else {
  const exportFn = \`
export const refreshAccessToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token');
  const response = await axiosInstance.post('/auth/refresh', { refreshToken });
  const { accessToken } = response.data;
  localStorage.setItem('accessToken', accessToken);
  return accessToken;
};
\`;
  content = content + exportFn;
  fs.writeFileSync(path, content, 'utf8');
  console.log('refreshAccessToken eklendi.');
}
"
```

**Not:** EÄer axios instance adÄ± `axiosInstance` deÄil `api` ya da baÅka bir isimse, Ã¶nce Åunu Ã§alÄ±ÅtÄ±r:
```bash
cat src/api/axios.js | grep "const " | head -10
```
Ve instance adÄ±nÄ± Ã¶Ären. YukarÄ±daki `node -e` komutundaki `axiosInstance` kÄ±smÄ±nÄ± gerÃ§ek isimle deÄiÅtir.

---

### AdÄ±m 4: DashboardReservations.jsx Kontrol Et

```bash
cat src/pages/DashboardReservations.jsx | head -40
```

`initialState` tanÄ±mÄ± var mÄ± kontrol et. Yoksa AdÄ±m 5'e geÃ§.

---

### AdÄ±m 5: DashboardReservations.jsx'e initialState Ekle

```bash
node -e "
const fs = require('fs');
const path = 'src/pages/DashboardReservations.jsx';
let content = fs.readFileSync(path, 'utf8');

if (content.includes('const initialState')) {
  console.log('initialState zaten mevcut, deÄiÅiklik yapÄ±lmadÄ±.');
} else {
  const initialStateDef = \`
const initialState = {
  search: '',
  filterType: 'All',
  filterStatus: 'All',
  sortType: 'Newest',
  currentPage: 1
};
\`;
  // Import bloÄunun hemen sonrasÄ±na ekle
  // Son import satÄ±rÄ±ndan sonra eklenecek
  const lastImportIdx = content.lastIndexOf('import ');
  const afterLastImport = content.indexOf('\n', lastImportIdx);
  content = content.slice(0, afterLastImport + 1) + initialStateDef + content.slice(afterLastImport + 1);
  fs.writeFileSync(path, content, 'utf8');
  console.log('initialState eklendi.');
}
"
```

---

### AdÄ±m 6: DeÄiÅiklikleri DoÄrula

```bash
echo "=== axios.js son 20 satÄ±r ==="
tail -20 src/api/axios.js

echo "=== DashboardReservations.jsx ilk 50 satÄ±r ==="
head -50 src/pages/DashboardReservations.jsx
```

Her iki dosyada da eklenen kodlar gÃ¶rÃ¼nmeli.

---

### AdÄ±m 7: Build Test Et (Opsiyonel ama Ãnerilen)

```bash
npm run build 2>&1 | tail -30
```

Build baÅarÄ±lÄ±ysa hata giderildi demektir.

---

## Etkilenen Dosyalar

| Dosya | DeÄiÅiklik |
|-------|-----------|
| `src/api/axios.js` | `refreshAccessToken` export ekleme |
| `src/pages/DashboardReservations.jsx` | `initialState` const tanÄ±mlama |

## BaÅarÄ± Kriterleri

- `axios.js` dosyasÄ±nda `export const refreshAccessToken` satÄ±rÄ± mevcut
- `DashboardReservations.jsx` baÅÄ±nda `const initialState = {` mevcut
- `npm run build` hatasÄ±z tamamlanÄ±yor

## Riskler

- Node.js kurulu deÄilse `node -e` Ã§alÄ±Åmaz â alternatif: `python3 -c "..."` kullan
- Windows'ta path separator sorunu â `/c/Users/...` yerine `C:\Users\...` dene
- Instance adÄ± farklÄ±ysa AdÄ±m 3'teki `axiosInstance` dÃ¼zeltilmeli

<commit_message>fix: refreshAccessToken ve initialState shell komutuyla eklendi</commit_message>
